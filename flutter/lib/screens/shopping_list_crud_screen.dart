import 'package:flutter/material.dart';

class ShoppingListCrudScreen extends StatelessWidget {
  const ShoppingListCrudScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
